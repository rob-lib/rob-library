import { ImgBubbleDirective } from './img-bubble.directive';

describe('ImgBubbleDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgBubbleDirective();
    expect(directive).toBeTruthy();
  });
});
