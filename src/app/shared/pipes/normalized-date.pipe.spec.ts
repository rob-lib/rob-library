import { NormalizedDatePipe } from './normalized-date.pipe';

describe('NormalizedDatePipe', () => {
  it('create an instance', () => {
    const pipe = new NormalizedDatePipe();
    expect(pipe).toBeTruthy();
  });
});
